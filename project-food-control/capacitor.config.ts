import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'project-food-control',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
